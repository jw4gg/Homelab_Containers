name: Update Service Inventory

on:
  push:
    branches:
      - main
    paths:
      - "**/compose.yaml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pyyaml --quiet

      - name: Generate SERVICES.md
        run: python .github/scripts/generate_inventory.py

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git diff --quiet SERVICES.md 2>/dev/null && git ls-files --error-unmatch SERVICES.md 2>/dev/null; then
            echo "SERVICES.md unchanged, skipping commit."
          else
            git add SERVICES.md
            git commit -m "chore: update service inventory [skip ci]"
            git push origin main
          fi
